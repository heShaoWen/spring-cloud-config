spring:
        profiles:
                active:
                - dev
---
spring:
        profiles: dev
        application:
                name: spring-cloud-dept-dev

---
spring:
        profiles: test
        application:
                name: spring-cloud-dept-test
#  请确保为UTF-8格式

